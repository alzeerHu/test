import React from "react";
import MainBlockView from "./View";
const MainBlockEdit = (props) => {
  return (

    <div>
        <MainBlockView {...props}/>
    </div>
  )
};

export default MainBlockEdit;